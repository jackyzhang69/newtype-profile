{
  "version": "spousal-baseline",
  "fields": [
    { "name": "full_name", "type": "string", "required": true },
    { "name": "date_of_birth", "type": "date", "required": true },
    { "name": "nationality", "type": "string", "required": false },
    { "name": "document_id", "type": "string", "required": false },
    { "name": "issue_date", "type": "date", "required": false },
    { "name": "expiry_date", "type": "date", "required": false },
    { "name": "relationship_start_date", "type": "date", "required": false },
    { "name": "cohabitation_start_date", "type": "date", "required": false }
  ],
  "checks": [
    "cross_document_identity",
    "timeline_consistency",
    "cohabitation_consistency",
    "signature_presence",
    "page_completeness"
  ]
}
